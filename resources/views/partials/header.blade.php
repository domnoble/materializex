<header class="{{ get_option( 'mx_header_class' ) }}">
    <div class="container valign-wrapper">
      <img class="logo" src="@asset( get_option( 'mx_logo_image' ) )"/>
      <div class="brand">
        <h1 class="white-text">{{ get_bloginfo('name', 'display') }}</h1>
        <p class="flow-text white-text">{{ get_bloginfo('description', 'display') }}</p>
      </div>
    </div>
</header>
